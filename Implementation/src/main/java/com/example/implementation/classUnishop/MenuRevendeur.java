package com.example.implementation.classUnishop;

public class MenuRevendeur {
    Systeme systeme;
    Session session;

    /**Constructeur de la Classe MenuAcheteur
     *
     */
    MenuRevendeur(Systeme systeme,Session session){
        this.systeme=systeme;
        this.session=session;
    }
    /*
    public void vendre(Panier panier, Coordonnees infos){
        if (!(this.session.isExpired())) {

            try {
                Revendeur revendeur = this.session.user();

    }
    */
}